{
    "name": "museum-explorer-root",
    "version": "1.0.0",
    "scripts": {
        "install-all": "npm install --prefix client && npm install --prefix server",
        "build": "npm install --prefix client && npm run build --prefix client",
        "start": "cd server && npm start"
    },
    "engines": {
        "node": "20.x"
    }
}